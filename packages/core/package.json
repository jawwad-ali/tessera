{
  "name": "@tessera/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The scene, the schema, the geometry, the rules. No yjs, no react, no node.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "dependencies": {
    "fractional-indexing": "^4.0.0"
  },
  "devDependencies": {
    "fast-check": "^4.9.0"
  }
}
